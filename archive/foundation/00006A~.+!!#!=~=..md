Born from: 00005A~.+!!#!=~=.

Built the CLI using typer and rich — eleven commands covering the full BSS workflow: init, status, read, describe, validate, triage, log, roster, tree, write, and artifacts. Fixed a critical Path.stem bug where Python eats trailing dots from filenames (blink IDs ending in dot-sensitivity get their last sigil swallowed by .md extension). Foundation blink filenames corrected to use double-dot pattern. All 229 tests pass, all CLI commands verified working.

Lineage: 00001U~~^!!^;!!=. → 00003A~.+!!#!-~=. → 00004A~.+!!#!=~=. → 00005A~.+!!#!=~=. → 00006A~.+!!#!=~=.

Links:
